#!/usr/bin/env bash
# Wanda + PGD recovery on google/gemma-2-9b-it on the chemistry domain (full variant).
# See ./README.md for the matrix; per-script knobs come from config_full.yaml.
# Generated by experiments/baselines_2026_04_30/ generator — do not hand-edit.

set -euo pipefail

# Run from the repo root regardless of the script's invocation cwd.
cd "$(dirname "$0")/../.."

python experiments/baselines_2026_04_29/sweep_wanda.py \
  --config experiments/baselines_2026_04_30/config_full.yaml \
  --model-id google/gemma-2-9b-it \
  --dataset-subset chemistry \
  --devices cuda:3,cuda:6 \
  -s 0.4,0.5,0.6,0.7 \
  --enable-llm-judge --enable-wandb --enable-pgd
